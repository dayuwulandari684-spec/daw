# Install semua dengan: pip install -r requirements.txt
# Lalu: playwright install chromium

# Browser automation (scraping + upload)
playwright>=1.42.0

# Image & video processing
Pillow>=10.2.0
numpy>=1.26.0

# Text-to-Speech (gratis, tidak perlu API key)
gTTS>=2.5.1
pydub>=0.25.1          # normalisasi volume audio (opsional)

# Warna terminal
colorama>=0.4.6

# HTTP request (backup scraping)
requests>=2.31.0

# Env file
python-dotenv>=1.0.1

# Cari foto produk otomatis (tanpa API key)
duckduckgo-search>=6.2.0
